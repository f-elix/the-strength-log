import { authMachine } from "./authMachine";
import { searchMachine } from "./searchMachine";
import { sessionMachine } from "./sessionMachine";

export const machines = [authMachine, searchMachine, sessionMachine];
